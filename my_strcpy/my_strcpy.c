#include <stddef.h>

char *my_strcpy(char *dest, const char *source)
{
    size_t i = 0;
    while (s[i] != '\0')
    {
        dest[i] = source[i];
        i++;
    }
    dest[i + 1] = '\0';
    return res;
}
